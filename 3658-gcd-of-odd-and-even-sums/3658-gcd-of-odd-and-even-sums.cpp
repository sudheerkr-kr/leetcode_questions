class Solution {
public:
    int gcdOfOddEvenSums(int n) {
        // int sumOdd = n * n;
        // int sunEven = n * (n+1);

        // gcd(n * n, n*(n + 1));
        // //n * gcd(n, n + 1);
        // //n;
        return n;
    }
};